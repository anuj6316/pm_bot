---
phase: 11-rest-api-for-react-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - backend/backend/settings.py
autonomous: true
requirements:
  - REQ-001
  - REQ-002

must_haves:
  truths:
    - "DRF 3.17.1 is installed and verified"
    - "django-cors-headers 4.9.0 is installed"
    - "djangorestframework-simplejwt 5.5.1 is installed"
    - "JWT authentication configured with short-lived access tokens"
    - "CORS middleware positioned correctly in MIDDLEWARE"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Dependency declarations for DRF stack"
      contains: "djangorestframework, django-cors-headers, djangorestframework-simplejwt"
    - path: "backend/backend/settings.py"
      provides: "REST_FRAMEWORK and SIMPLE_JWT configuration"
      exports: ["REST_FRAMEWORK", "SIMPLE_JWT", "CORS_ALLOWED_ORIGINS"]
  key_links:
    - from: "backend/backend/settings.py"
      to: "INSTALLED_APPS"
      via: "REST framework apps registered"
      pattern: "rest_framework|rest_framework_simplejwt|corsheaders"
---

<objective>
Install and configure Django REST Framework stack with verified versions, JWT authentication, and CORS for separate domain deployment.

Purpose: Establish the foundation for secure API endpoints that React frontend can consume with proper authentication and cross-origin support.

Output: Configured DRF stack with production-safe defaults for JWT token lifetime, CORS origins, and authentication classes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-rest-api-for-react-app/11-RESEARCH.md
@backend/backend/settings.py
@backend/pyproject.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install DRF stack with verified versions</name>
  <files>backend/pyproject.toml, backend/requirements.txt</files>
  <action>
    Update pyproject.toml dependencies to use exact versions from RESEARCH.md:
    - djangorestframework==3.17.1 (March 2026, Django 6.0 + Python 3.14 support)
    - django-cors-headers==4.9.0 (Sept 2025, supports Django 4.2-6.0)
    - djangorestframework-simplejwt==5.5.1 (July 2025, no known vulnerabilities)
    
    Run: `uv sync` in backend directory to install dependencies.
    Verify installation: `pip show djangorestframework`, `pip show django-cors-headers`, `pip show djangorestframework-simplejwt`
    
    Note: Dependencies already exist in pyproject.toml but without version pins. Add exact version pins.
  </action>
  <verify>
    <automated>cd /home/mindmap/Desktop/pm_bot/backend && uv sync && pip show djangorestframework | grep "Version: 3.17.1"</automated>
  </verify>
  <done>
    All three packages installed with exact versions (3.17.1, 4.9.0, 5.5.1). uv sync completes without errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Configure JWT authentication with secure defaults</name>
  <files>backend/backend/settings.py</files>
  <action>
    Update SIMPLE_JWT configuration in settings.py per RESEARCH.md "SimpleJWT Configuration (2026 Secure Defaults)":
    
    - ACCESS_TOKEN_LIFETIME: timedelta(minutes=5) — short-lived to reduce token theft window
    - REFRESH_TOKEN_LIFETIME: timedelta(days=7) — longer-lived for user convenience
    - ROTATE_REFRESH_TOKENS: True — security: new refresh token on each use
    - BLACKLIST_AFTER_ROTATION: True — invalidate old refresh tokens (required per user decision)
    - ALGORITHM: "HS256"
    - SIGNING_KEY: Use separate JWT_SIGNING_KEY from env (fall back to SECRET_KEY for dev)
    - AUTH_HEADER_TYPES: ("Bearer",)
    
    Update REST_FRAMEWORK configuration:
    - DEFAULT_AUTHENTICATION_CLASSES: JWTAuthentication
    - DEFAULT_PERMISSION_CLASSES: IsAuthenticated
    - DEFAULT_PAGINATION_CLASS: PageNumberPagination
    - PAGE_SIZE: 20
    
    Ensure token_blacklist app is added to INSTALLED_APPS for blacklisting support.
  </action>
  <verify>
    <automated>cd /home/mindmap/Desktop/pm_bot/backend && python manage.py check</automated>
  </verify>
  <done>
    Django check passes. SIMPLE_JWT has 5-min access token, 7-day refresh token, rotation enabled, blacklisting enabled. token_blacklist app in INSTALLED_APPS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Configure CORS for separate domain deployment</name>
  <files>backend/backend/settings.py</files>
  <action>
    Update CORS configuration per RESEARCH.md "CORS Configuration (Production-Safe)":
    
    1. Verify MIDDLEWARE order: CorsMiddleware MUST be before CommonMiddleware
       Current order in settings.py line 52: corsheaders.middleware.CorsMiddleware is correctly positioned.
    
    2. Add CORS_ALLOW_HEADERS to include "authorization" header for JWT:
       CORS_ALLOW_HEADERS = [*corsheaders.defaults.default_headers, "authorization", "content-type"]
    
    3. Update CORS_ALLOWED_ORIGINS to load from environment variable:
       CORS_ALLOWED_ORIGINS = env.list("CORS_ALLOWED_ORIGINS", default=["http://localhost:3000"])
       Note: Will need python-decouple or django-environ for env.list, or use os.getenv with split
    
    4. Set CORS_ALLOW_CREDENTIALS = True (required for HttpOnly cookies)
    
    5. NEVER use CORS_ALLOW_ALL_ORIGINS = True
    
    Since settings.py uses os.getenv pattern, use: 
    CORS_ALLOWED_ORIGINS = os.getenv("CORS_ALLOWED_ORIGINS", "http://localhost:3000").split(",")
    
    Per user decision "Separate domain (api.pm-bot.com for backend)", the frontend will be on different origin.
  </action>
  <verify>
    <automated>cd /home/mindmap/Desktop/pm_bot/backend && python manage.py check</automated>
  </verify>
  <done>
    Django check passes. CORS middleware correctly ordered, CORS_ALLOWED_ORIGINS loads from env, CORS_ALLOW_CREDENTIALS=True, authorization header allowed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React frontend → Django API | Untrusted input crosses here — JWT validation required |
| External network → API endpoint | Public exposure — rate limiting, CORS enforcement needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | JWT authentication | mitigate | Short-lived access tokens (5 min), HttpOnly refresh cookies, token blacklisting enabled |
| T-11-02 | Information Disclosure | JWT token storage | mitigate | Access token in React memory only (not localStorage), refresh token in HttpOnly cookie |
| T-11-03 | Spoofing | CORS configuration | mitigate | Explicit CORS_ALLOWED_ORIGINS from env, never `*`, credentials enabled |
| T-11-04 | Denial of Service | List endpoints | mitigate | Pagination enabled (PAGE_SIZE=20), select_related/prefetch_related in Wave 2 |
| T-11-05 | Tampering | Auth endpoint brute force | accept | Low risk for internal tool; can add rate limiting in future phase if needed |
</threat_model>

<verification>
- `pip show djangorestframework` returns Version: 3.17.1
- `pip show django-cors-headers` returns Version: 4.9.0
- `pip show djangorestframework-simplejwt` returns Version: 5.5.1
- `python manage.py check` passes without errors
- SIMPLE_JWT.ACCESS_TOKEN_LIFETIME = 5 minutes
- SIMPLE_JWT.BLACKLIST_AFTER_ROTATION = True
- CORS middleware positioned before CommonMiddleware
- token_blacklist app in INSTALLED_APPS
</verification>

<success_criteria>
- [ ] DRF 3.17.1, django-cors-headers 4.9.0, djangorestframework-simplejwt 5.5.1 installed
- [ ] JWT authentication configured with 5-min access token, 7-day refresh token
- [ ] Token blacklisting enabled (ROTATE_REFRESH_TOKENS=True, BLACKLIST_AFTER_ROTATION=True)
- [ ] CORS configured with explicit origins, credentials enabled, authorization header allowed
- [ ] Django system check passes
- [ ] token_blacklist app added to INSTALLED_APPS
</success_criteria>

<output>
After completion, create `.planning/phases/11-rest-api-for-react-app/11-01-SUMMARY.md`
</output>
