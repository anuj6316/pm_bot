---
phase: 11-rest-api
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/v1/viewsets/issue.py
  - backend/api/v1/serializers/issue.py
  - backend/api/v1/urls.py
autonomous: true
requirements:
  - REQ-003
must_haves:
  truths:
    - "GET /api/issues/ returns Plane issues"
  artifacts:
    - path: "backend/api/v1/viewsets/issue.py"
      provides: "Issue retrieval"
    - path: "backend/api/v1/serializers/issue.py"
      provides: "Issue schema"
---

<objective>
Implement Issue API endpoints.

Purpose: Provide the React dashboard with the list of issues from Plane to trigger agent sessions.
Output: Working /api/issues/ endpoint.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create IssueSerializer and IssueViewSet</name>
  <files>backend/api/v1/serializers/issue.py, backend/api/v1/viewsets/issue.py</files>
  <action>
    Create `IssueSerializer` to serialize `plane_client.models.Issue` (or equivalent).
    Create `IssueViewSet` that retrieves issues from Plane client.
  </action>
  <verify>
    <automated>curl /api/issues/</automated>
  </verify>
  <done>Issue API endpoints implemented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Issue data is internal |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Info Disclosure | Issues endpoint | mitigate | Ensure proper auth and filtering |
</threat_model>

<verification>
Automated: API response check
</verification>

<success_criteria>
- GET /api/issues/ returns correctly formatted list of issues
</success_criteria>
