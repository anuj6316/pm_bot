---
phase: 11-rest-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/v1/viewsets/session.py
  - backend/api/v1/urls.py
autonomous: true
requirements:
  - REQ-001
  - REQ-002
must_haves:
  truths:
    - "GET /api/sessions/ returns a list of agent sessions"
    - "POST /api/sessions/{id}/sync/ triggers a sync process"
  artifacts:
    - path: "backend/api/v1/viewsets/session.py"
      provides: "Sync trigger endpoint"
    - path: "backend/api/v1/urls.py"
      provides: "Endpoint registration"
  key_links:
    - from: "backend/api/v1/viewsets/session.py"
      to: "deep_agent.tasks.sync_session"
      via: "POST action on session"
---

<objective>
Implement REST API endpoints for sessions and manual sync.

Purpose: Allow React dashboard to monitor agent state and trigger manual intervention (sync) if needed.
Output: Working /api/sessions/ CRUD and sync endpoints.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
# From backend/api/v1/viewsets/session.py
class SessionViewSet(viewsets.ReadOnlyModelViewSet):
    ...
    # TODO: Add action='sync' here
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sync action to SessionViewSet</name>
  <files>backend/api/v1/viewsets/session.py</files>
  <action>
    Add a custom `@action(detail=True, methods=['post'])` named `sync` to `SessionViewSet`.
    This action should call the `deep_agent.tasks.trigger_manual_sync.delay(session_id)` task.
    Return 202 Accepted on success.
  </action>
  <verify>
    <automated>curl -X POST /api/sessions/{id}/sync/</automated>
  </verify>
  <done>Session sync endpoint implemented and callable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Authenticated users only, but should enforce owner-based access |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Elevation | SessionViewSet | mitigate | Ensure user session check in sync action |
</threat_model>

<verification>
Automated: run tests in `backend/tests/api/`
</verification>

<success_criteria>
- GET /api/sessions/ exists
- POST /api/sessions/{id}/sync/ triggers the background task and returns 202
</success_criteria>
