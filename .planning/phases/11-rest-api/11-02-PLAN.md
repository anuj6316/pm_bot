---
phase: 11-rest-api
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/api/v1/urls.py
  - backend/api/v1/viewsets/session.py
autonomous: true
requirements:
  - REQ-002
must_haves:
  truths:
    - "GET /api/sessions/{id}/logs/ returns reasoning log stream"
  artifacts:
    - path: "backend/api/v1/viewsets/session.py"
      provides: "Logs SSE stream endpoint"
  key_links:
    - from: "backend/api/v1/viewsets/session.py"
      to: "django.http.StreamingHttpResponse"
      via: "logs action"
---

<objective>
Implement streaming reasoning logs for sessions.

Purpose: Allow React dashboard to stream agent reasoning process in real-time.
Output: SSE endpoint /api/sessions/{id}/logs/.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add logs streaming action to SessionViewSet</name>
  <files>backend/api/v1/viewsets/session.py</files>
  <action>
    Add `@action(detail=True, methods=['get'])` named `logs` to `SessionViewSet`.
    Implementation should return a `StreamingHttpResponse` wrapping a generator that yields logs from the session's log file or database.
    Use text/event-stream content type.
  </action>
  <verify>
    <automated>curl -N /api/sessions/{id}/logs/</automated>
  </verify>
  <done>SSE logs endpoint implemented and streaming</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Streaming logs sensitive information |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Info Disclosure | Logs endpoint | mitigate | Ensure only authorized users access logs |
</threat_model>

<verification>
Automated: curl command check for event-stream content
</verification>

<success_criteria>
- GET /api/sessions/{id}/logs/ streams reasoning data
</success_criteria>
